{% extends "base.html.twig" %}
{% block title %}
Formulaire Connexion Comptable
{% endblock %}
{% block body %}
    <html>
        <style>
            body{
                background-image:url({{asset('img/91.jpg')}});
            }
           
        </style>
        <body>
           
            <div>
                    <nav class="navbar navbar-expand-lg navbar-light bg-light">
                  <a class="navbar-brand" href="#">GSB</a>
                  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                  </button>
                 
                  <div class="collapse navbar-collapse" id="navbarNavDropdown">
                    <ul class="navbar-nav">
                      <li class="nav-item active">
                        <a class="nav-link" href="{{ path('add_art') }}">Ajoute Article<span class="sr-only">(current)</span></a>
                      </li>
                      <li class="nav-item">
                        <a class="nav-link" href="{{ path('add_categ') }}">Ajoute Catégories</a>
                      </li>
                      <li class="nav-item">
                        <a class="nav-link" href="{{ path('listeart') }}">Articles</a>
                      </li>
                      <li class="nav-item">
                        <a class="nav-link" href="#">Liste Catégorie</a>
                      </li>
                      
                    </ul>
                  </div>
                  <div style="float: right" type="button" class="btn btn-outline-danger" >
                      <a href="{{ path('login') }}" >Déconexion</a>
                  </div>
                </nav>
            </div>
        </body>
    </html>
{% endblock %}